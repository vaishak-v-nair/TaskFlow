[build]
builder = "nixpacks"
buildCommand = "npm ci --cache /tmp/.npm --prefer-offline --no-audit && npm run build"

[deploy]
startCommand = "npm run migrate && npm start"
healthcheckPath = "/api/auth/me"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[networks]
private = true

[build.env]
NODE_VERSION = "20.19"
